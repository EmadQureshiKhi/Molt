"""Capture surfaces that turn agent activity into events on an engineer machine.

Nothing in this subpackage holds a cluster credential; every write leaves the
machine over the collector's authenticated interface.
"""
